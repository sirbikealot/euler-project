# euler_project_problem_3.rb

=begin

The prime factors of 13195 are 5, 7, 13 and 29.

What is the largest prime factor of the number 600851475143 ?

find all factors of a number n (13195)

=end

n = 13195

factors = []
least_test_factor = 2
def factorize(i)
	until least_test_factor <= n/2
		unless factors.include?(least_test_factor)
			if n % least_test_factor == 0
				factors.push(least_test_factor) # append least_test_factor to factors array
				# attempt to factorize next least_test_factor
			else
				least_test_factor += 1
				# attempt to factorize next least_test_factor
			end
		else
			least_test_factor += 1
			# attempt to factorize next least_test_factor
		end
	end
end
			


